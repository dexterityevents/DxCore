dx.lang = "en" -- the default language of all modules (this can be changed client side)
dx.forcelang = false -- does the client HAVE to use the server lang?
dx.colorscheme = "default" -- the default color scheme of all modules (this can be changed client side)
dx.forcecolorscheme = false -- does the client HAVE to use the server color scheme?